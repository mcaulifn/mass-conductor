{
  "name": "mass-conductor",
  "version": "0.2.0",
  "description": "Home Assistant Lovelace card for Music Assistant: pick room / player / source, now-playing + browse/search. Path B: a bundled HA integration holds the MA connection server-side; the card talks only to HA.",
  "type": "module",
  "scripts": {
    "build": "node esbuild.mjs",
    "watch": "node esbuild.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "home-assistant",
    "lovelace",
    "music-assistant",
    "custom-card"
  ],
  "license": "MIT",
  "dependencies": {
    "lit": "^3.2.0"
  },
  "devDependencies": {
    "custom-card-helpers": "^1.9.0",
    "esbuild": "^0.28.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.11"
  }
}
